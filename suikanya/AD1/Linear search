public class ls_number{
    public static Ls_Iterative(int[]A,int n,int k){
        for (i = 1;i<=n;i++){
        if (A[i]==k)
        return i ;
        else 
    }
          return -1;
    }
    public static int Ls_Recursive(int[]A,int n,int k){
        if (n >A.length-1)
         return -1;
         if (A[k])
    
}
}
